export function FlightLogFieldPresenter() {
  // Only the static debug-definition hook is needed by the parser adapter.
}

FlightLogFieldPresenter.adjustDebugDefsList = function adjustDebugDefsList() {
  // No-op in this project. The full presenter is only needed for viewer UI labelling.
};
